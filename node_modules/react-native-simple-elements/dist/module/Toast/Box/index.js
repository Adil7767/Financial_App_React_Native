import styled from "styled-components/native";
import { background, border, color, compose, flexbox, layout, position, shadow, space } from "styled-system";
export const systemProps = compose(space, layout, color, flexbox, background, border, position, shadow);
const Box = styled.View(systemProps);
export default Box;
Box.defaultProps = {
  display: "flex",
  justifyContent: "center",
  position: "relative"
};
//# sourceMappingURL=index.js.map